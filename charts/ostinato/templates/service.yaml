apiVersion: v1
kind: Service
metadata:
  name: {{ include "ostinato.fullname" . }}
  labels:
    {{- include "ostinato.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "ostinato.selectorLabels" . | nindent 4 }}
